import 'package:flutter/material.dart';

class MigrarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Migrar"),
      ),
      body: Center(
        child: Text("Pantalla de Migración"),
      ),
    );
  }
}
